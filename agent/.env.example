# =============================================================================
# 复制成 .env 后填入自己的值：
#
#     cp agent/.env.example agent/.env
#
# agent/.env 已在 .gitignore 中，不会被提交。控制台「运行时配置」弹窗会检查
# 下面三项是否齐全，缺任何一项都会拦在启动之前。
# =============================================================================

# ---- 教师模型接入（合成阶段必需）----
# 任何 OpenAI 兼容接口均可，填自己的 key、base_url 和模型名。
#
# API Key 故意留空：控制台判断「配没配好」看的是值非空，写成 sk-xxxx 之类的
# 占位符会被当成真 key，顶栏显示绿色的「模型接入 ✓」，跑起来却全是鉴权错误。
# 真实 key 形如 sk- 后面跟一长串字符，直接粘在这一行等号右边。
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
AGENT_DISTILL_MODEL=deepseek-chat

# ---- 联网搜索工具（可选）----
# agent/tools/search_web.py 需要 Exa 的 key。不配则该工具在执行阶段不可用，
# 其余流程不受影响。
#
# 注意：这里只认一个值。同一个变量写多行不会轮换（dotenv 只保留最后一行），
# 换 key 请直接改这一行的值。
EXA_API_KEY=
