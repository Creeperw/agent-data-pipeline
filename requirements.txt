# 流水线与控制台共用的依赖。
#
# 版本按下限约束，不锁死上限。实际验证过的版本为：
#   openai 2.29 / tqdm 4.67 / transformers 5.8 / fastapi 0.135
#   uvicorn 0.44 / python-dotenv 1.2 / exa-py 2.12 / datasketch 1.9

openai>=1.0
tqdm>=4.60
transformers>=4.30
fastapi>=0.100
uvicorn>=0.23
python-dotenv>=1.0
exa-py>=1.0

# 可选：去重阶段使用 MinHash 时需要。缺失时 deduplicate.py 会自动回落到精确匹配。
datasketch>=1.6
